# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'AdServe Dashboards'
    orgId: 1
    folder: 'AdServe'
    folderUid: 'adserve'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
